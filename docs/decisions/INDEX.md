# Decision Records — Index

Every ratified decision lives in this directory. The keeper (`claude-code`) maintains both the individual records and this index. Per the Conclave Charter §Decision Records, *"significant work closes with a record of what was chosen, why, what was rejected, known risks, open questions, and who is keeping continuity."*

## All decisions, newest first

| # | Date | Title | Mode | Outcome |
|---|---|---|---|---|
| **[0024](0024_dashboard_plugin_system.md)** | 2026-05-23 | Dashboard plugin system (frontend-only, v1) | Glen-directed | `app/dashboard/plugin_loader.js` defines `window.Plugins` with four extension-point arrays (`sidebarTabs`, `inboxRowActions`, `inboxFilters`, `detailPanels`); manifest at `/static/plugins/manifest.json` is fetched on `DOMContentLoaded` and each listed script registers contributions. Four delimited hooks in `dashboard.js` (sidebar render, `switchView` dispatch, inbox row build, inbox filters UI, detail render) no-op when no plugins are registered. Per-plugin `api` object exposes `el` / `fetchJSON` / `refreshInbox` / `openDetail` / read-only state / namespaced log; reserved-id guard prevents core-view collisions. Demo at `app/dashboard/plugins/example-hello.js`; failure-cause detail panel and trajectory tools plugin ship on the same branch as first real consumers |
| **[0023](0023_trajectory_exporter.md)** | 2026-05-23 | Trajectory exporter | Glen-directed | `app/services/trajectory_exporter.py::export_trajectory(task_id)` writes a single-line JSONL per task at `<exports_root>/trajectories/<task_id>.jsonl` containing prompts, transcript, per-run timings/tokens/cost, final result (incl. `action_plan` and `failure_cause_tags`), decision, confidence aggregate, `parent_task_id`, `protocol_version`, `exported_at`. Schema pinned at `_TRAJECTORY_SCHEMA_VERSION = 1`; `export_all_terminal()` for bulk sweep with per-task error isolation; new `trajectories_root()` in `app/utils/paths.py`. Orchestrator `_post_finalize_hooks` calls the exporter after every terminal status flip (try/except wrapped — never fails the task). New endpoints `POST /api/tasks/{task_id}/trajectory/export` and `POST /api/trajectories/export-all` (sibling router from `app/main.py`). 12 new tests |
| **[0022](0022_failure_cause_tags.md)** | 2026-05-23 | Failure-cause tags | Glen-directed | New `FailureCause` enum in `app/protocol/validators.py` (9 members: `missing_evidence`, `tool_timeout`, `bad_json_output`, `premise_conflict`, `multimodal_perception_split`, `unresolved_dissent`, `repetition_loop_backstop`, `clarification_unanswered`, `permission_denied`); new `FinalResult.failure_cause_tags`. Rule-based, no-LLM classifier `app/services/trace_analyzer.py::classify_failure_causes(...)` inspects status, agreement level, errors, multimodal-disagreement messages, approval denials, repetition-guard signals. Orchestrator calls it from a new `_post_finalize_hooks(task_id)` after the terminal status flip and after `_save_final_result` writes the row; defensively wrapped so a buggy rule cannot fail a task. Persisted in additive `final_results.failure_cause_tags_json`; surfaced via `_row_to_final_result`; Decision Memory entries gain a forward-compatible `failure_cause_tags` field. `PROTOCOL_VERSION` 1.1 → 1.2. 29 new tests |
| **[0021](0021_evidence_norms_charter_v1_3.md)** | 2026-05-21 | Charter v1.3: Evidence Norms | conclave 3-AI + Glen ratified | Charter amended; participants must cite or identify the basis for load-bearing factual claims, label unsupported assumptions, and preserve material evidence gaps. |
| **[0020](0020_clarification_gate_and_draft_artifacts.md)** | 2026-05-20 | Clarification Gate and Draft Artifacts | Glen-directed | Adds a consult-mode numbered clarification pause and app-owned draft artifacts for file/edit/patch handoff; supported artifacts can be explicitly applied to `project_path` by the user. |
| **[0019](0019_structured_action_plan.md)** | 2026-05-20 | Structured Action Plan | Glen-directed | Adds `FinalResult.action_plan` as an advisory structured operational handoff compiled from final `recommended_actions`; classifies steps, required permissions, policy status, and reasons without executing, pausing, or creating approvals. |
| **[0018](0018_no_sandbox_manifest_for_cli_seats.md)** | 2026-05-18 | CLI seats receive sandbox path but not the file manifest | Glen-directed (ratified at implementation greenlight) | `_format_project_sandbox(task, include_manifest=True)` gains the toggle; CLI adapters (codex/claude-code/gemini) pass `False` because they have native file tools (`-C`, `--add-dir`, `--include-directories`); OpenRouter seats pass `True` (default) — they need the manifest to navigate the sandbox. Saves 5–30 KB per CLI prompt, the second-order contribution to the gemini-timeout pattern that Batch C already capped via the budget centralizer. 5 new tests, 337 total |
| **[0017](0017_per_seat_readiness.md)** | 2026-05-18 | Per-seat readiness + configurable CLI command paths | Glen-directed (ratified at implementation greenlight) | New `Readiness` model on `BaseAdapter` reports `(available, reason, hint)` per seat; `/api/health` adds a `seats: [...]` array with 30-second cache; new `AgentConfig.command_path` field lets the user paste an absolute path that wins over `shutil.which()` — closes the GUI-launch-doesn't-inherit-shell-PATH gap surfaced by the packaging consult thread. `configured_path_missing` is strict: a wrong `command_path` does NOT silently fall back to PATH. Reasons: `ok`, `command_not_found`, `configured_path_missing`, `api_key_missing`, `readiness_check_failed`. 21 new tests, 316 total |
| **[0016](0016_user_data_root_and_lazy_config.md)** | 2026-05-18 | User-data root abstraction + lazy config resolution | Glen-directed (ratified after conclave pressure-test) | Single platform-aware `user_data_root()` primitive routes the SQLite DB, sandboxes, pidlock, uploads, exports, logs, and (outside dev mode) `config.yaml` through one resolver. `app/main.py`'s import-time `config = load_config()` becomes a lazy `get_config()`. First-run migration uses `VACUUM INTO` + active-pid check + atomic `.tmp` rename batch to move repo-relative state into the new root; originals are preserved. Dev-mode fallback (walk-up `pyproject.toml` + `config.example.yaml` detection) keeps current `./data/` workflow intact when running from source. Three conclave-driven fixes folded in at ratification: uploads/exports scope, migration race-safety, narrowed Operability Impact |
| **[0015](0015_tool_loop_api_seats.md)** | 2026-05-17 | Tool-loop architecture for the API-based council seats | Glen-directed | Adds an OpenAI-style tool-call loop (`read_file` + `list_dir` + `glob`) to `OpenRouterAdapter` so the open-weight seats can ask for specific files on demand instead of getting everything inlined. Per-seat opt-in via `openrouter.models[].tool_loop: false` default. New `tool_call` / `tool_result` message types. Bounds: 8 iterations / 256 KiB cumulative read per turn; `glob` capped at 200 paths per call. Ratified 2026-05-17; implementation following |
| **[0014](0014_remove_ollama_cloud_adapter.md)** | 2026-05-16 | Remove the Ollama Cloud adapter (supersedes DR0009 in part) | Glen-directed | `OllamaCloudAdapter`, `OllamaCloudConfig`/`OllamaCloudModel`, the `OLLAMA_API_KEY` settings slot, the Ollama Cloud Settings panel row, and Ollama mentions across user-facing docs all removed. DR0009/DR0011 narrative preserved as audit history; DR0009 carries a supersession banner. OpenRouter is now the sole pluggable open-weight backing |
| **[0013](0013_prefetched_url_attachments.md)** | 2026-05-16 | Pre-fetched URL attachments — bounded live-web access for the conclave (shared-snapshot shape, not per-agent web tools) | Glen-directed (spec; ratification pending) | New `Context.urls[]` field; `app/services/url_fetcher.py` fetches each URL once, server-side, before dispatch; all participants see the same inlined bytes. Per-agent independent web tool access rejected for v1; may be revisited |
| **[0012](0012_inline_sandbox_for_api_adapters.md)** | 2026-05-12 | Inline the project sandbox for the API-based council seats | Glen-directed | OpenRouter / Ollama seats now get a read-only file tree + file contents inlined into their prompt when a task has a sandbox (they can't browse files). `app/utils/sandbox_inline.py` |
| **[0011](0011_openrouter_council_seats.md)** | 2026-05-12 | OpenRouter council seats (pay-per-token open-weight models) | Glen-directed | `deepseek` / `glm` / `qwen` seats now OpenRouter-backed (no subscription); Ollama Cloud adapter kept but disabled by default; `OPENROUTER_API_KEY` added to the Settings panel |
| **[0010](0010_settings_panel_and_db_api_keys.md)** | 2026-05-12 | Left-rail Settings panel + DB-stored API keys (env-fallback) | Glen-directed | Dashboard left rail → gear → Settings → API Keys; Ollama key stored in DB with eyeball reveal; rule: env var wins, else DB |
| **[0009](0009_ollama_cloud_council_seats.md)** | 2026-05-12 | Ollama Cloud council seats (open-weight models: deepseek / glm / qwen) — **superseded by DR0014 (2026-05-16)** | Glen-directed | New `OllamaCloudAdapter` (pluggable, config-driven); seats appear in the dashboard checkbox list; auth via `OLLAMA_API_KEY`. Established the pluggable-seat pattern. Adapter and config removed in DR0014; OpenRouter (DR0011) is now the sole open-weight backing |
| **[0008](0008_export_detail_pdf_docx_text.md)** | 2026-05-12 | Export task detail as PDF / DOCX / Markdown / Text from the dashboard | Glen-directed | New `GET /api/tasks/{id}/download?format=...` endpoint + dashboard control; browser Save dialog for destination; `reportlab` + `python-docx` added |
| **[0007](0007_codex_gemini_slash_command_parity.md)** | 2026-05-11 | Codex + Gemini slash-command parity + provenance tracking | Glen-directed | 8 slash commands now invokable from Codex and Gemini sessions; `source_agent` round-trips through API; first capability decision under Charter v1.2 |
| **[0006](0006_charter_v1_2_operability_before_capability.md)** | 2026-05-11 | Charter v1.2: *Operability before capability* principle + Decision Records *Operability Impact* field | conclave 3-AI + Glen ratified | Charter amended; new principle binding on every future capability/infrastructure deliberation |
| **[0005](0005_db_concurrency_and_tier2_archive.md)** | 2026-05-11 | DB concurrency hardening (busy_timeout + with_retry) + Tier 2 export/archive tracking | Glen-directed | `busy_timeout=30s` + retry wrapper; `exported_at` columns + bulk export endpoint + dashboard surface |
| **[0004](0004_sandbox_not_layer2.md)** | 2026-05-11 | Project Sandbox shipped; Layer 2 (in-conclave write/execute) deferred | Glen ↔ Claude (keeper) | Sandbox shipped; Layer 2 explicitly not built |
| **[0003](0003_retention_policy.md)** | 2026-05-11 | DB retention policy: two-part metric (operational trigger + tier-based selection) | conclave 3-AI + Glen ratified | Implemented in `app/services/retention.py`; 2 GB / 1,000 task budget |
| **[0002](0002_multimodal_disagreement_policy.md)** | 2026-05-11 | Charter v1.1: Multimodal Disagreement section (do not synthesize visual perception disputes) | conclave 3-AI + Glen ratified | Charter amended; binding on every prompt |
| **[0001](0001_charter_adoption.md)** | 2026-05-10 | Initial adoption of Conclave Charter v1.0 | conclave 3-AI + Glen ratified | Charter embedded in every prompt via `skills/generic/conclave_charter.md` |

## How to read this index

Each row shows the decision's date, title, the deliberation mode that produced it, and the operational outcome. Click into any record for the full structured record with rejected alternatives, known risks, and open questions.

## How a new decision lands here

Per the Charter's amendment process:

1. **Propose**: a conclave-mode task on Switchboard deliberates the proposed change
2. **Ratify**: Glen records a `/decide` decision on the task
3. **File**: the keeper writes `docs/decisions/<NNNN>_<slug>.md` with the structured record
4. **Index**: the keeper appends a row to this file
5. **Cross-link**: relevant docs that change as a result of the decision get updated and reference the decision number

Decisions are append-only. Earlier decisions can be superseded by later ones (e.g., a v1.3 amendment to the Charter updates the current binding version while older amendment records stay in place as audit history).

## The conclave's own dogfooding

Five of these decision records came out of the conclave deliberating about its own design (charter adoption, multimodal disagreement, retention, charter v1.2, charter v1.3). The rest were Glen-directed. The Switchboard product is, in part, a tool for designing itself. This index is part of the audit trail of that process.
